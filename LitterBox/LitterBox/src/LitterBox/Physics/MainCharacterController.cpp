#include "LitterBox/Engine/Input.h"
#include "LitterBox/Physics/PhysicsMath.h"
#include "LitterBox/GameLogic/GameLogic.h"

	//INPUT->SubscribeToKey(func, KeyCode::KEY_W, KeyEvent);

	//LB::INPUT->SubscribeToKey(func, KeyCode::KEY_W, KeyEvent);
/*
	class MainCharacterController : public GameLogic {
	public:
		LB::Vec2<float>GetPos();
		LB::Vec2<float>GetVel();

		LB::Vec2<float>Move();

	private:
		LB::Vec2<float> position;
		LB::Vec2<float> velocity;

		bool activeCollision;

		RigidBody* attachedRigidBody;
		//Collider attachedCollider;
	};

// Basically I need to create a class that allows me to handle everything
// that happens to the main character, for example
// Taking Damage and Reducing the Hearts
// Amount of balls with the character
// The upgrades associated with the character

	LB::Vec2<float> MainCharacterController::GetPos() 
	{
		return position 
	}
*/